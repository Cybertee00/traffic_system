# Environment variables template for Smart License API
# Copy this file to config.env and fill in your values

# Database URL for Supabase
# Get this from: Supabase Dashboard → Settings → Database → Connection string
# Use "Connection pooling" mode for production
# Format: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@[HOST]:[PORT]/postgres
# IMPORTANT: URL-encode special characters in password:
#   @ → %40
#   # → %23
#   % → %25
#   / → %2F
#   : → %3A
# Example: postgresql://postgres.xxx:MyP%40ss%23123@aws-0-us-east-1.pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres

# For local development, you can use:
# DATABASE_URL=postgresql://postgres:0000@localhost:5432/postgres

# For production (Render), DATABASE_URL will be set via Render's environment variables
# No need to set it in config.env for production

